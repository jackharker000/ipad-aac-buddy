# Parley env. Copy to `.env` and fill in.
#
# Server-only vars (no VITE_ prefix) are read by /api/* routes and server fns
# via process.env.X. They must also be added to the Vercel project's
# environment settings for production / preview deploys.
#
# Public vars with the VITE_ prefix get bundled into the client JS and are
# visible in any browser network inspector — never put a secret behind a
# VITE_ prefix.

# --- Firebase (client / PUBLIC) -----------------------------------------------
# The web app's Firebase config. These VITE_-prefixed values are PUBLIC BY
# DESIGN: they only identify the Firebase project to the client SDK — they are
# not secrets. Security is enforced by Firebase Authentication + Firestore /
# Storage security rules, not by hiding these values. Copy them from the
# Firebase console: Project Settings → General → Your apps → SDK setup.
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:0000000000000000000000
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# --- Firebase (server / SECRET) -----------------------------------------------
# Service-account credential for the Firebase Admin SDK. Used server-side only
# for admin operations (the cross-device user list, promoting the first account
# to admin) and the waitlist Firestore write. NEVER VITE_-prefix this — it is a
# private key and must not ship in the browser bundle.
#
# Generate it: Firebase Console → Project Settings → Service accounts →
# Generate new private key (downloads a JSON file), then base64-encode it:
#   base64 -i serviceAccount.json
# Paste the resulting single-line string here (and into the Vercel env).
FIREBASE_SERVICE_ACCOUNT_B64=

# --- LLM providers (SECRET, server-only) --------------------------------------
# Default provider is Claude; OpenAI is the switchable alternative — set the
# key for whichever provider(s) you actually want.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Model selection per tier — server picks the model from request.tier
# ("fast" for live suggestions / expand; "smart" for summaries, drafts,
# event prep, profile enrichment).
PARLEY_ANTHROPIC_FAST_MODEL=claude-haiku-4-5
PARLEY_ANTHROPIC_SMART_MODEL=claude-sonnet-4-6
PARLEY_OPENAI_FAST_MODEL=gpt-4o-mini
PARLEY_OPENAI_SMART_MODEL=gpt-4o

# --- STT (SECRET, server-only) ------------------------------------------------
# ElevenLabs Scribe (revisit Apple on-device after the Capacitor wrap).
# ELEVENLABS_API_KEY=

# --- TTS (SECRET, server-only) ------------------------------------------------
# Same ELEVENLABS_API_KEY as STT for ElevenLabs Flash v2.5; separate key for the
# Cartesia Sonic 3 latency fallback.
# CARTESIA_API_KEY=

# James's voice clone ID (ElevenLabs voice_id). Settings → People → James.
PARLEY_JAMES_VOICE_ID=

# --- App config / proxy hardening (both OPTIONAL; unset == prior behavior) ----
# The /api/* proxy holds the keys above, so on a public URL it's an open relay
# unless locked down. These two knobs raise the bar without a full auth system.

# Comma-separated allow-list of browser origins. When set, the API reflects a
# request's Origin only if it matches (otherwise echoes the first entry, so a
# mismatched browser is blocked by CORS). Unset => Access-Control-Allow-Origin
# stays "*" (fine for local dev / first deploy). Example:
#   PARLEY_ALLOWED_ORIGIN=https://parley.example.com,https://parley.vercel.app
PARLEY_ALLOWED_ORIGIN=

# Shared secret gating every /api/* route. When set, requests must send header
# `x-parley-token: <this value>` or get a 401. Unset => no gate.
# NOTE: for a public PWA the token ships in the client JS, so this only raises
# the bar; the real trust boundary is the Capacitor native wrap (token in the
# app binary, not on a public URL).
PARLEY_CLIENT_TOKEN=

# Comma-separated email allow-list auto-promoted to admin on sign-in (Firebase
# custom claim `admin: true`). Set on the server (Vercel) only. Unset falls
# back to a built-in default that lists the project owner, so the cross-device
# admin dashboard works out of the box for the first admin.
#   PARLEY_ADMIN_EMAILS=you@example.com,teammate@example.com
PARLEY_ADMIN_EMAILS=
